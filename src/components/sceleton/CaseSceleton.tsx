export default function CaseSceleton() {
  return (
    <div
      style={{
        width: "100%",
        maxWidth: "800px",
        minHeight: "150px",
        backgroundColor: "var(--sceleton-color)",
      }}
    ></div>
  );
}
